---
title: "Is it hot right now?"
---

```{ojs}
//| label: load-map-data
import { aq, op } from "@uwdata/arquero"

bucket_base = "https://isithot-data.s3.ap-southeast-2.amazonaws.com/www/"

stats = await aq.loadJSON(bucket_base + "stats/stats_all.json")
stats

places = await aq.loadJSON("locations.json")
places

// join the two by station id
```

```{ojs}
//| label: draw-map

```
